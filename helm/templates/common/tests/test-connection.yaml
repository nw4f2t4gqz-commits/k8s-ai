apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "k8sgpt-ai-analyzer.fullname" . }}-test-connection"
  labels:
    {{- include "k8sgpt-ai-analyzer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "k8sgpt-ai-analyzer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
